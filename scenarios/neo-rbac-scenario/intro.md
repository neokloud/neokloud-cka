# CKADmock – RBAC Error in Deployment

A pod within the Deployment named neo-depl and in namespace neo is logging errors. 

Look at the logs identify errors messages. Find errors, including User "system:serviceaccount:neo:default"

cannot list resource "deployment" [...] in the namespace neo' 

Update the Deployment neo-depl to resolve the errors in the logs of the Pod. 

The dev-deployment’s manifest can be found at /home/files/neo-depl.yaml
